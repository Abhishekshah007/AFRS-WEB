// iconMap.ts
import {
    FileText, FlaskConical, FolderOpen, Rss,
    BookCopy, PlayCircle, Microscope,
    // Education section icons
    Monitor, Award, Timer, BookOpen, GraduationCap,
    Beaker, Globe, Library, FileStack, Laptop, BarChart2,
} from 'lucide-react'
import type { LucideIcon } from 'lucide-react'

export const iconMap: Record<string, LucideIcon> = {
    // Resource cards
    FileText,
    FlaskConical,
    FolderOpen,
    Rss,
    BookCopy,
    PlayCircle,
    Microscope,
    // Education cards
    Monitor,       // Training Online
    Award,         // Forensic Professional Course
    Timer,         // Capsule Training
    BookOpen,      // Research / Project
    GraduationCap,
    Globe,
    Library,
    FileStack,
    Laptop,
    BarChart2,
}